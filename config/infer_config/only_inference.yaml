do_infer: True
debug: True  # Set true or use INFERENCE_DEBUG=1 to enable _dbg prints
metrics_device: cuda:0 # Device for rollout metric computation (e.g. cuda:0, xpu:0, cpu). Null = auto (cuda:0/xpu:0 if available, else cpu)
inference_directory: /home/maxsa/cfd_bench/sample_inference/infer_streaming_test
dataset_directory_path: null  # if null, will use the dataset_directory_path from data_config.json inside the experiment directory.
batch_eval_metrics: True
metrics_batch_size: 4
filter_features:
  infer_filter_frames: [null, null]
  infer_filter_groups: ["seed 000000"]
n_infer_rollouts: 5
n_infer_plot_examples: 0
infer_from_random_timestep: True
infer_from_ic: True