model_name: ConvNeXt

# Core architecture
latent_channels: 32
n_layers: 4

# Optional settings (blocks keep their own defaults)
blocks_per_stage: null
blocks_at_neck: null
drop_path: 0.0
layer_scale_init_value: 1e-6

# Framework-level normalization fields (required by load_model)
norm_layer_eps: 1e-5
norm: 'layer' # layer, batch, group, identity