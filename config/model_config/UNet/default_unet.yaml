#model_config:
model_name: UNet
latent_channels: 64
activation_fn_name: gelu
norm_layer_eps: 1e-5
norm: 'group' # layer, batch, group
num_groups_div_rate: 2
channel_multiplier: [1,2,2,4]
is_attn: [False,False,False,False]
mid_attn: False
n_blocks: 2 #Number of DownBlock/UpBlock in each level
use1x1: False